/**
 * @deprecated This module has been split for Fast Refresh cleanliness:
 *   - constants  -> ./theme.js   (CHART, axisProps, hoverCursor, lineCursor)
 *   - component  -> ./ChartTooltip.jsx
 *
 * Import from those paths directly. This file is intentionally left empty and
 * is no longer referenced anywhere (module resolution prefers ./theme.js).
 */
export {};
