/**
 * Re-exports the `usePlant` hook from PlantContext.
 * Enables a cleaner import path: `import { usePlant } from '@/hooks'`
 * instead of the verbose `import { usePlant } from '../context/PlantContext'`.
 */
export { usePlant } from '@/context/PlantContext';
